### MINERS HIVE CONFIG ################################

#URL where hive server is
HIVE_HOST_URL="${HIVE_HOST_URL}"
API_HOST_URLs="${API_HOST_URLs}"

#Id of the rig, numeric value from admin panel
RIG_ID=${RIG_ID}

#Rig password as in admin panel
RIG_PASSWD="${RIG_PASSWD}"

#Rig hostname
WORKER_NAME="${WORKER_NAME}"

#Id of the farm
FARM_ID=${FARM_ID}

#Selected miners
MINER=
MINER2=

#Rig timezone
TIMEZONE="${TIMEZONE}"

#Watchdog
WD_ENABLED=0